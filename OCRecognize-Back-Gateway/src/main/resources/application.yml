server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
        - id: authenticationService
          uri: http://localhost:8081/
          predicates:
            - Path=/ms-authentication/**